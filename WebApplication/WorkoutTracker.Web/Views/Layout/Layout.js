define('Views/Layout', ['backbone'], function (Backbone) {
    return Backbone.View.extend({

        render: function () {
            // var navBar = new NavBar();
            // navBar.render();
        }

    });
});